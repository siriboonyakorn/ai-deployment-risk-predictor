# ML dependencies - install separately once the ML model milestone begins
# These require Python 3.13 or earlier for pre-built wheels.
# On Python 3.14+, install via conda or wait for official wheel releases.
#
# pip install -r requirements-ml.txt

scikit-learn>=1.6.0
numpy>=2.2.1
pandas>=2.2.3
